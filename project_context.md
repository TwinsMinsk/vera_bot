Инструкции для ИИ-разработчика (Context)

Ты — профессиональный Python-разработчик, специализирующийся на aiogram 3.x.
Твоя задача: Развернуть проект бота на основе предоставленного Технического Задания, следуя архитектуре antigravity-kit.

Ключевые принципы разработки:

Модульность: Не пиши весь код в одном файле. Разноси логику по сервисам.

Асинхронность: Все I/O операции (запросы к API, БД) должны быть async/await.

Безопасность: Токены и ключи брать ТОЛЬКО из переменных окружения (os.getenv).

Обработка ошибок: Бот не должен падать.

Детали реализации:

Безопасность и Доступ (Admin & Security)

Whitelist (ADMIN_IDS): Бот реагирует ТОЛЬКО на админов.

Команды: /start, /clear.

Память (Redis)

Ключ: chat_history:{user_id}. await redis.aclose() для закрытия.

Сервисы AI

1. Текстовый интеллект (OpenRouter + Tavily Search)

Провайдер LLM: OpenRouter (deepseek/deepseek-chat).

Поиск в Интернете (SearchService) - TAVILY API:

Ключ: os.getenv("TAVILY_API_KEY").

Логика (PIPELINE):

Получить сообщение от юзера.

Если есть ключевые слова ("погода", "новости", "курс", "кто такой"), сделать запрос в TavilyClient.

DEBUG: Вывести в консоль print(f"DEBUG SEARCH: {results}").

INJECTION (ВАЖНО): Если результаты есть, модифицировать system_prompt или добавить новое системное сообщение:
Role: System, Content: "FACTS FROM INTERNET:\n{results}\nUse these facts to answer the user."

Отправить историю + этот контекст в LLM.

Anti-Hallucination:

Если Tavily вернул пустоту/ошибку -> LLM должна сказать "Не нашел информации".

Если Tavily вернул данные -> LLM ОБЯЗАНА использовать их, а не говорить "у меня нет доступа".

2. Голосовой интеллект (Groq)

Провайдер: Groq Cloud (whisper-large-v3-turbo).

🚨 Исправления (CRITICAL FIXES)

1. Форматирование текста (HTML)

В методе message.answer(...) всегда parse_mode="HTML".

Функция очистки текста должна корректно превращать Markdown ** в <b> и не ломать теги.